{
  "name": "witus-agent-evals",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-agnostic eval harness for WitUS LangGraph agents: property-based cases, deterministic checks + LLM-as-judge, regression detection, provider comparison.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "evals": "tsx cli.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
